# PDFsharp.OpenType

This is the PDFsharp assembly that handles OpenType fonts.
It it used by both XGraphics and the new PDFsharp Graphics.

The API is public, but in the **PdfSharp.Internal.OpenType** namespace.
It is not intended to be used from PDFsharp customers and may be changed without notice.

<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <PropertyGroup>
    <Product>MigraDoc</Product>
    <Copyright>$(CopyrightMigraDoc)</Copyright>
  </PropertyGroup>

</Project>

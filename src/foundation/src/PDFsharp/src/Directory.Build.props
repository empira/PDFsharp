<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <PropertyGroup>
    <Product>PDFsharp</Product>
    <Copyright>$(CopyrightPdfSharp)</Copyright>
  </PropertyGroup>

</Project>
